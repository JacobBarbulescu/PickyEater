version: '3.8'

services:
  mongo:
    image: mongo
    container_name: picky_mongo
    ports:
      - "27017:27017"

  redis:
    image: redis/redis-stack
    container_name: picky_redis
    ports:
      - "6379:6379"
      - "8001:8001"